name: Reusable Deploy
on: workflow_dispatch                       # Here we can make it callable from another workflow
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo " Deploying & Uploading"